# Figures

Publication-figure generation. Reads only cached CSVs from `results/` — safe to
re-run any time (`make figures`). Outputs land in `paper/figures/` (fig*.png)
and `analysis_plots/` (NN_*.png).
