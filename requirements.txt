# Core scientific stack
numpy>=1.26
pandas>=2.0
scipy>=1.11
matplotlib>=3.7
seaborn>=0.13
statsmodels>=0.14
scikit-learn>=1.3

# Tree-based and boosting models
xgboost>=2.0
catboost>=1.2
lightgbm>=4.0

# Hyperparameter optimization
optuna>=3.4

# Explainability
shap>=0.44

# Bayesian inference (Sections 6, 7)
pymc>=5.10
arviz>=0.17
pytensor>=2.18

# Symbolic regression
# PySR additionally requires Julia (https://pysr.readthedocs.io)
pysr>=0.18

# Notebook
jupyter>=1.0
ipython>=8.18
nbconvert>=7.10

# Report generation
python-docx>=1.1

# Excel input
openpyxl>=3.1

# Testing (regression checks on canonical numbers)
pytest>=7.4

# Development hooks (notebook output stripping, whitespace, YAML check)
# See .pre-commit-config.yaml; one-time setup: `pre-commit install`
pre-commit>=3.5
nbstripout>=0.7

# Bibliography validation (optional, only used by paper authors)
# bib-check-cli  # available via pipx from ~/tools/bib-check
